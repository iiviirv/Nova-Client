<?xml version="1.0" encoding="utf-8"?>
<!-- A small, resizable status widget. minWidth/Height target a 4x1 cell so the
     status line has room; it stays legible when shrunk to 2x1. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_nova"
    android:previewLayout="@layout/widget_nova"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen" />
